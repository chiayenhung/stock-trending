# Synthesis task

Synthesize the validated analyst outputs. Every proposal must trace to claim_ids
and include entry, stop or invalidation, target or trailing rule, time exit, and
expiry. Do not choose final quantity. Return the requested JSON schema.
