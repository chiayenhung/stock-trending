# Synthesis task

Synthesize the analyst outputs into research-only trend signals. Every signal
must trace to claim_ids and include an assessment, research horizon, thesis, and
monitoring triggers. Set `research_only` to true and `validation_status` to
`pending`; deterministic code owns the final validation status. Do not produce
transaction instructions, price levels, or quantities. Return the requested
JSON schema.
