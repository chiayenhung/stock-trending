# Claude Cowork adapter

Claude-produced structured output is validated by the OpenAI Responses API
adapter. Same-vendor fallback is prohibited. Provider credentials and data
sharing controls are configured outside prompts.
