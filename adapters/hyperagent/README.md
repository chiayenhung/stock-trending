# Hyperagent adapter

Not active. Confirm the platform agent, model, tool, scheduling, sandbox, and
credential surfaces before implementing this research-only adapter. It must not
introduce transaction capabilities. Any future mail connector must honor the
HTML `body_mime_type`, preserve attachments, and exclude non-passing signals
from ranked research sections.
